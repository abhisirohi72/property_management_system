services:
  ci3:
    build: .
    container_name: property-management-system
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    extra_hosts:
      - "host.docker.internal:host-gateway"  
    restart: unless-stopped
